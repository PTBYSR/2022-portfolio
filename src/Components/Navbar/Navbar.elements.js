import styled from 'styled-components';

export const Nav = styled.nav`
    padding-top: 12px;
    justify-content: end;
    align-items: center;
    position: relative;
    top: 0;
    z-index: 999;
`;