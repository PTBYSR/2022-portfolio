import styled from 'styled-components';

export const Nav = styled.nav`
    margin-top: 9px;
    justify-content: end;
    align-items: center;
    position: relative;
    top: 0;
    z-index: 999;
    margin: 0;
`;