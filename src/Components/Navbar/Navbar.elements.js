import styled from 'styled-components';

export const Nav = styled.nav`
    height: 80px;
    justify-content: center;
    align-items: center;
    position: relative;
    top: 0;
    z-index: 999;
`;